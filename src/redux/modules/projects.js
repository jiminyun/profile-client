// imports

// actions

// action creatroes

// initial state
const initialState = {
  project: {}
};
// reducer
const reducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

// reducer functions

//exports
export default reducer;
